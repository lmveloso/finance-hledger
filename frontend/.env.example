# Em dev, Vite usa o proxy pra /api (vide vite.config.js)
# Em produção (build servido pelo FastAPI), é same-origin — nenhuma variável necessária.
# Só configure abaixo se for hospedar o frontend SEPARADO do backend no futuro:

# VITE_API_URL=https://finance-api.sua-tailnet.ts.net
